# W0.5 — vfio_user_wire cleanup（Message/WireMessage 拆分 + irq 决策 pure 化 + NoopTransport 迁出）

> **执行方式：inline（控制者本人执行，非 subagent-driven）。** architect review A-1 命中：subagent-driven 默认每-task-commit 与本 plan 单-commit 硬冲突。改为 inline 执行——每 Track 内分步改 + cargo test 自验，全程不 commit，最后 F-5 一次性精确 pathspec commit；inline 下控制者掌握每条 git 命令，无 subagent 裸 commit 吞 DECISIONS.md 之险。
>
> **Plan type: refactor-with-existing-tests。** 不强制 RED-first；oracle = 现有 vfio_user_wire + vfio_user_transport + nvme_of_tcp_target lib tests 不退化 + POC-1/2 真跑 PASS。新增 pure-fn 单测显式标。**baseline 起手必实测，不信 plan 写死的数字。**
>
> **⚠️ 单 commit 纪律（用户 2026-06-11 明确）**：整个 W0.5 三 Track 合成**一个 commit**，不按 Track/step 拆。见 [[commit-granularity-coarse-not-per-step]]。

**Goal:** 收尾 W0 留下的三件 cleanup：(A) `framing::Message` 拆出 sans-IO `WireMessage{header,payload}`（wire crate）+ transport 留 `Message{wire,fds}`；(B) `irq::handle_set_irqs` 的纯决策抽成 wire crate 的 `decide_set_irqs_action`；(C) `NoopTransport` 从 vfio_user_transport 迁到 pcie_device_core，解除 nvme_of_tcp_target 对整个 vfio_user_transport 的反向依赖。

**Architecture:** 三 Track 互相独立（依赖只到 cargo build 层，不互锁）。wire crate 再添 `framing`（WireMessage 类型）+ `irq`（决策）两个 sans-IO 模块；transport 端 `Message` 用 `Deref<Target=WireMessage>` 让所有 `msg.header`/`msg.payload` caller 零改动；NoopTransport 落到 pcie_device_core（保持 dep-free，silent stub）。

**Tech Stack:** Rust 2024，rustc 1.95；zerocopy 0.8 + thiserror 1.0；现有 `cargo test` + POC-1/2 oracle。

**Spec 来源：** `docs/superpowers/specs/2026-06-11-openhcl-vfio-user-vtl2-firmware-design.md` §9 audit（Message 拆分）；W0 plan 末尾 Open risks。

**前置 commit：** `1213e951`（W0 squash 后单 commit）。

**回退指引：** W0.5 全程未 commit；任何 Track 失败 `git checkout -- <file>` 回滚该文件后重做；全乱则 `git stash` 或 `git checkout -- usnvmemu/`（注意别动别会话的 `usnvmemu/docs/DECISIONS.md`）。

---

## Baseline（起手必跑）

```bash
cd /home/xp/refs/openvmm/usnvmemu/crates/vfio_user_wire && cargo test --lib 2>&1 | tail -2
cd /home/xp/refs/openvmm/usnvmemu/crates/vfio_user_transport && cargo test --lib 2>&1 | tail -2
cd /home/xp/refs/openvmm/usnvmemu/crates/nvme_of_tcp_target && cargo test --lib 2>&1 | tail -2
```

预期：wire **31**、transport **69**、nvme_of_tcp_target **~172**（实测记真值，后续不退化）。

---

## 执行顺序：C → B → A

理由（explore 结论）：C 最隔离（迁文件 + 改 imports，不动逻辑），先解 nvme_of_tcp_target 反向 dep；B 在 wire 加新文件 + 改 transport 一个 fn，无 cross-file 涟漪；A 涟漪最广（5 文件），最后做。

---

## Track C — NoopTransport 迁到 pcie_device_core

**Files:**
- Create: `usnvmemu/crates/pcie_device_core/src/noop_transport.rs`
- Modify: `usnvmemu/crates/pcie_device_core/src/lib.rs`（加 `mod` + `pub use`）
- Delete: `usnvmemu/crates/vfio_user_transport/src/transport.rs`
- Modify: `usnvmemu/crates/vfio_user_transport/src/lib.rs`（删 `pub mod transport` + `pub use transport::NoopTransport`）
- Modify: `usnvmemu/crates/nvme_of_tcp_target/src/{session.rs,async_session.rs}` + `tests/v8e7_2_async_fabric.rs`（`vfio_user_transport::NoopTransport` → `pcie_device_core::NoopTransport`）
- Modify: `usnvmemu/crates/nvme_of_tcp_target/Cargo.toml`（删 `vfio_user_transport` dep）

**设计决定（architect D-1 修正）**：NoopTransport 落 pcie_device_core 后 **panic stub**（不是 silent，也不引 tracing）。理由：
- pcie_device_core 当前**零依赖**，不能为 NoopTransport 引 tracing 破坏纯度；
- **但 silent return 0 是错的**——nvme_of_tcp_target 有 NoopTransport 被误接到 IO 路径致 silent-DMA-drop bug 的历史（session.rs:604/1639/1693 注释 + 断言测试记录了"guest 写 SQ tail doorbell 被 NoopTransport 静默丢弃"的旧 bug）。silent stub 会抹掉当年抓到那个 bug 的诊断信号。
- 故三个方法都 `panic!`：happy-path（合法用法从不调用它，如 nvme_of_tcp V2 Property Set 只写寄存器）零开销；任何误接到真 IO 路径的调用**立即 loud fail**，比 warn 更强且零依赖。V2 路径已 review 确认从不调 dma_*/fire_interrupt，panic 不会在生产触发。
- pcie_device_core 已有 `CaptureTransport` 作 test helper 先例，NoopTransport 同源。

- [ ] **C-1: 写 pcie_device_core/src/noop_transport.rs**

```rust
// Copyright (c) Microsoft Corporation.
// Licensed under the MIT License.

//! `NoopTransport` —— panic 桩 [`Transport`]：任何方法被调用即 panic。
//!
//! 用途：device 实现需要一个 `Transport` ctx，但当前路径**保证不调用**
//! `ctx.dma_*` / `ctx.fire_interrupt`（如 nvme_of_tcp_target V2 的 Property
//! Set / CC.EN 寄存器写路径——只改寄存器状态，不发起 DMA / 中断）。这种
//! 场景用本桩占位。
//!
//! **为何 panic 而非 silent**：本桩曾在 nvme_of_tcp_target 被误接到 IO 路径，
//! 导致 guest doorbell 的 DMA 被静默丢弃（极难发现的 bug）。silent return 0
//! 会抹掉诊断信号；panic 让任何"本不该调用却调用了"的误用立即暴露。合法
//! 用法（从不调用）零开销。
//!
//! 生产数据路径**不**用它：真 Transport 由具体 transport crate 实现
//! （vfio_user_transport 的 `VfioUserSession` 走 vfio-user wire；nvme_of_tcp
//! 的 fabric session 走 R2T/H2CData）。
//!
//! 从 vfio_user_transport 迁来（W0.5，2026-06-11）——解除 nvme_of_tcp_target
//! 仅为 1 个 ZST 拉整个 vfio_user_transport 的反向依赖。panic 而非 tracing
//! 以保持 pcie_device_core 零依赖。

use crate::Transport;

/// panic 桩 [`Transport`]：任何方法被调用即 panic。仅用于"device 从不调用
/// ctx.dma_* / fire_*"的路径占位 / 单测 / 示例；误接到真 IO 路径会 loud fail。
pub struct NoopTransport;

impl Transport for NoopTransport {
    fn fire_interrupt(&mut self, _msix_index: u32) {
        panic!(
            "NoopTransport.fire_interrupt called — 本桩仅用于从不发起中断的路径；\
             误用了请接真 Transport"
        );
    }
    fn dma_read(&mut self, _gpa: u64, _len: u32) -> u64 {
        panic!(
            "NoopTransport.dma_read called — 本桩仅用于从不发起 DMA 的路径；\
             误用了请接真 Transport（历史上此处 silent-drop 过 guest doorbell DMA）"
        );
    }
    fn dma_write(&mut self, _gpa: u64, _data: Vec<u8>) -> u64 {
        panic!(
            "NoopTransport.dma_write called — 本桩仅用于从不发起 DMA 的路径；\
             误用了请接真 Transport"
        );
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn noop_is_object_safe() {
        let mut t = NoopTransport;
        let _dyn_ref: &mut dyn crate::Transport = &mut t;
    }

    #[test]
    #[should_panic(expected = "NoopTransport.dma_read called")]
    fn noop_dma_read_panics_to_catch_misuse() {
        let mut t = NoopTransport;
        let _ = t.dma_read(0xCAFE, 4096);
    }

    #[test]
    #[should_panic(expected = "NoopTransport.fire_interrupt called")]
    fn noop_fire_interrupt_panics_to_catch_misuse() {
        let mut t = NoopTransport;
        t.fire_interrupt(7);
    }
}
```

- [ ] **C-2: pcie_device_core/src/lib.rs 加 mod + re-export**

在 `pub mod describe;` 一类声明附近加 `mod noop_transport;`，在 `pub use device::Transport;` 附近加：

```rust
pub use noop_transport::NoopTransport;
```

验证：
```bash
cd /home/xp/refs/openvmm/usnvmemu/crates/pcie_device_core && cargo test --lib 2>&1 | tail -3
```
预期：原 test 数 + 2（noop 两个），全 passed。

- [ ] **C-3: 删 vfio_user_transport/src/transport.rs + lib.rs 引用**

```bash
rm /home/xp/refs/openvmm/usnvmemu/crates/vfio_user_transport/src/transport.rs
```
编辑 `usnvmemu/crates/vfio_user_transport/src/lib.rs`：删 `pub mod transport;`（L50）和 `pub use transport::NoopTransport;`（L72）两行。

验证编译：
```bash
cd /home/xp/refs/openvmm/usnvmemu/crates/vfio_user_transport && cargo build --lib 2>&1 | tail -3
```
预期：Finished（vfio_user_transport 内部不依赖 NoopTransport，应直接通）。

- [ ] **C-4: nvme_of_tcp_target 改 import + 删 dep**

3 处 `vfio_user_transport::NoopTransport` → `pcie_device_core::NoopTransport`：
- `src/session.rs:1109`
- `src/async_session.rs:816`
- `tests/v8e7_2_async_fabric.rs:323`

更新 `src/session.rs:1106` 附近注释（`NoopTransport 只在 V2 验证安全`）——保留语义，把"vfio_user_transport"措辞改为"pcie_device_core"来源。

`usnvmemu/crates/nvme_of_tcp_target/Cargo.toml`：删 `vfio_user_transport = { path = "../vfio_user_transport" }`（L11）及其上方 L9 的 V2 注释。确认 `pcie_device_core` 已是直接 dep（应已在；若无则加 `pcie_device_core = { path = "../pcie_device_core" }`）。

验证：
```bash
cd /home/xp/refs/openvmm/usnvmemu/crates/nvme_of_tcp_target && \
  cargo build --lib 2>&1 | tail -3 && \
  cargo test --lib 2>&1 | tail -3
```
预期：build clean + lib test 数不退化（~172）。

- [ ] **C-5: Track C 回归**

```bash
cd /home/xp/refs/openvmm/usnvmemu/crates/nvme_of_tcp_target && \
  cargo test --test v8e7_2_async_fabric 2>&1 | tail -3
```
预期：integration test passed。Track C 完成，**不 commit**。

---

## Track B — decide_set_irqs_action pure 化

**Files:**
- Create: `usnvmemu/crates/vfio_user_wire/src/irq.rs`
- Modify: `usnvmemu/crates/vfio_user_wire/src/lib.rs`（加 `pub mod irq;`）
- Modify: `usnvmemu/crates/vfio_user_transport/src/irq.rs`（handle_set_irqs 改用 wire 决策）

**设计**：把 `handle_set_irqs`（transport/irq.rs:102-209）的纯决策抽成 wire crate 的 `decide_set_irqs_action(idx, flags, start, count, fd_count) -> SetIrqsAction`。transport 内 `handle_set_irqs` 先做 payload-len + decode（已在），再 `match decide_set_irqs_action(...)` 分发 IO（vectors mutation + reply/err）。

**决策分支**（严格保留现有优先级 + 灰区语义）：
- `idx != MSIX` → `BestEffortNonMsix`
- `DATA_NONE & TRIGGER & count==0` → `ClearAll`（优先于 EVENTFD）
- `DATA_EVENTFD & TRIGGER & count>0 & fd_count==count` → `Assign`
- `DATA_EVENTFD & TRIGGER & fd_count==0`（含 count==0）→ `Deassign`
- `DATA_EVENTFD & TRIGGER & 0<fd_count<count` → `InvalidPartialFds`
- 其它 flag 组合（MASK/UNMASK）→ `BestEffortUnsupported`

- [ ] **B-1: 写 vfio_user_wire/src/irq.rs**

```rust
// Copyright (c) Microsoft Corporation.
// Licensed under the MIT License.

//! `DEVICE_SET_IRQS` 的 sans-IO 决策。
//!
//! 抽自 `vfio_user_transport::irq::handle_set_irqs`（W0.5，2026-06-11）。本模块
//! 只做"给定 idx/flags/start/count/fd_count → 该执行哪个动作"的纯决策；真正的
//! vector mutation + eventfd 收发 + reply/err 由 transport 端按返回的
//! [`SetIrqsAction`] 分发。
//!
//! flag 语义见 [`crate::proto::irq_set`] / [`crate::proto::pci_irq`]。

use crate::proto::irq_set;
use crate::proto::pci_irq;

/// `decide_set_irqs_action` 的决策结果。transport 端按此 match 执行 IO。
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum SetIrqsAction {
    /// idx != MSIX：当前只 wire MSI-X，其它 idx no-op + best-effort OK。
    BestEffortNonMsix,
    /// DATA_NONE + TRIGGER + count==0：清整个 vector 数组。
    ClearAll,
    /// DATA_EVENTFD + TRIGGER + count>0 + fd_count==count：把 fd 赋给
    /// [start, start+count) 槽位。
    Assign { start: u32, count: u32 },
    /// DATA_EVENTFD + TRIGGER + fd_count==0：[start, start+count) 全部 de-assign
    /// （清 None / 掩码；含 count==0 的空范围）。
    Deassign { start: u32, count: u32 },
    /// DATA_EVENTFD + TRIGGER + 0<fd_count<count：部分 fd（QEMU 不会这么发），非法。
    InvalidPartialFds { need: u32, got: u32 },
    /// 其它 flag 组合（MASK/UNMASK 等）：spec 允许 server 不实现，best-effort OK。
    BestEffortUnsupported,
}

/// 纯决策：给定已解码的 SET_IRQS 参数 + 收到的 fd 数，返回该执行的动作。
///
/// **优先级**（与原 handle_set_irqs if/else 链一致，必须保留）：
/// 1. 非 MSIX idx 早返
/// 2. DATA_NONE + count==0 + TRIGGER（优先于 DATA_EVENTFD）
/// 3. DATA_EVENTFD + TRIGGER：按 fd_count 分 Assign / Deassign / InvalidPartialFds
/// 4. 其它 → BestEffortUnsupported
///
/// **review 灰区（真 QEMU 11 oracle）**：DATA_EVENTFD 的 fd 数未必 == count——
/// vfio-user 把 `-1`（de-assign / 跳过）编码为不经 SCM_RIGHTS 传该槽位。合法只两种：
/// `fd_count==count`（assign）或 `fd_count==0`（全 de-assign）。`0<fd_count<count`
/// 才非法。
pub fn decide_set_irqs_action(
    idx: u32,
    flags: u32,
    start: u32,
    count: u32,
    fd_count: usize,
) -> SetIrqsAction {
    if idx != pci_irq::MSIX {
        return SetIrqsAction::BestEffortNonMsix;
    }
    let is_data_none = flags & irq_set::DATA_NONE != 0;
    let is_data_eventfd = flags & irq_set::DATA_EVENTFD != 0;
    let is_trigger = flags & irq_set::ACTION_TRIGGER != 0;

    if is_data_none && count == 0 && is_trigger {
        return SetIrqsAction::ClearAll;
    }
    if is_data_eventfd && is_trigger {
        let need = count as usize;
        if need > 0 && fd_count == need {
            return SetIrqsAction::Assign { start, count };
        }
        if fd_count == 0 {
            return SetIrqsAction::Deassign { start, count };
        }
        return SetIrqsAction::InvalidPartialFds {
            need: count,
            got: fd_count as u32,
        };
    }
    SetIrqsAction::BestEffortUnsupported
}

#[cfg(test)]
mod tests {
    use super::*;

    const FL_EVENTFD_TRIGGER: u32 = irq_set::DATA_EVENTFD | irq_set::ACTION_TRIGGER;
    const FL_NONE_TRIGGER: u32 = irq_set::DATA_NONE | irq_set::ACTION_TRIGGER;

    #[test]
    fn non_msix_returns_best_effort() {
        assert_eq!(
            decide_set_irqs_action(pci_irq::INTX, FL_EVENTFD_TRIGGER, 0, 1, 1),
            SetIrqsAction::BestEffortNonMsix
        );
    }

    #[test]
    fn data_none_count_zero_returns_clear_all() {
        assert_eq!(
            decide_set_irqs_action(pci_irq::MSIX, FL_NONE_TRIGGER, 0, 0, 0),
            SetIrqsAction::ClearAll
        );
    }

    #[test]
    fn eventfd_full_fds_returns_assign() {
        assert_eq!(
            decide_set_irqs_action(pci_irq::MSIX, FL_EVENTFD_TRIGGER, 2, 3, 3),
            SetIrqsAction::Assign { start: 2, count: 3 }
        );
    }

    #[test]
    fn eventfd_zero_fds_returns_deassign() {
        assert_eq!(
            decide_set_irqs_action(pci_irq::MSIX, FL_EVENTFD_TRIGGER, 0, 1, 0),
            SetIrqsAction::Deassign { start: 0, count: 1 }
        );
    }

    /// count==0 + EVENTFD（非 DATA_NONE）走 Deassign 空范围分支（不进 Assign，
    /// 因 need>0 不满足）——锁死原 handle_set_irqs 微妙等价。
    #[test]
    fn eventfd_count_zero_returns_deassign() {
        assert_eq!(
            decide_set_irqs_action(pci_irq::MSIX, FL_EVENTFD_TRIGGER, 0, 0, 0),
            SetIrqsAction::Deassign { start: 0, count: 0 }
        );
    }

    #[test]
    fn eventfd_partial_fds_returns_invalid() {
        assert_eq!(
            decide_set_irqs_action(pci_irq::MSIX, FL_EVENTFD_TRIGGER, 0, 3, 1),
            SetIrqsAction::InvalidPartialFds { need: 3, got: 1 }
        );
    }

    #[test]
    fn unsupported_flag_combo_returns_best_effort() {
        // 只 set 一个未识别 bit（既非 DATA_NONE+count0 也非 EVENTFD+TRIGGER）
        assert_eq!(
            decide_set_irqs_action(pci_irq::MSIX, irq_set::ACTION_TRIGGER, 0, 1, 0),
            SetIrqsAction::BestEffortUnsupported
        );
    }

    /// DATA_NONE 但 count>0（非 0）→ ClearAll 不命中（要求 count==0）→ 非 eventfd
    /// → BestEffortUnsupported。锁死 DATA_NONE 灰区（architect C-5）。
    #[test]
    fn data_none_count_nonzero_returns_best_effort() {
        assert_eq!(
            decide_set_irqs_action(pci_irq::MSIX, FL_NONE_TRIGGER, 0, 3, 0),
            SetIrqsAction::BestEffortUnsupported
        );
    }
}
```

注意：`irq_set` / `pci_irq` 子模块在 wire crate `proto` 内已 `pub mod`（实测 proto.rs L379 `pub mod pci_irq` / L395 `pub mod irq_set`，W0 整文件搬过去可达）。`use crate::proto::{irq_set, pci_irq}` 成立。

- [ ] **B-2: lib.rs 加 pub mod + 跑测**

`usnvmemu/crates/vfio_user_wire/src/lib.rs` 加 `pub mod irq;`（在 `pub mod handshake;` 附近）。更新 crate-level doc 列 `[irq]` 模块。

```bash
cd /home/xp/refs/openvmm/usnvmemu/crates/vfio_user_wire && cargo test --lib 2>&1 | tail -3
```
预期：31 + 8（新 irq 决策测）= **39 passed**。

- [ ] **B-3: transport/irq.rs handle_set_irqs 改用 wire 决策**

编辑 `usnvmemu/crates/vfio_user_transport/src/irq.rs` 的 `handle_set_irqs`（L102-209）。保留：payload-len 校验（L109-113）、decode（L114-120）、`flags/idx/start/count` 提取、debug log。把 L134-208 的 if/else 决策链替换为：

```rust
    use vfio_user_wire::irq::{decide_set_irqs_action, SetIrqsAction};
    match decide_set_irqs_action(idx, flags, start, count, msg.fds.len()) {
        SetIrqsAction::BestEffortNonMsix => {
            tracing::debug!(idx, "SET_IRQS for non-MSIX idx: no-op (best-effort OK)");
            reply_ok(stream, msg_id, no_reply, "no-op")
        }
        SetIrqsAction::ClearAll => {
            vectors.vectors.clear();
            tracing::info!("SET_IRQS: cleared all MSI-X vectors");
            reply_ok(stream, msg_id, no_reply, "clear")
        }
        SetIrqsAction::Assign { start, count } => {
            let need = count as usize;
            let upto = (start as usize) + need;
            if vectors.vectors.len() < upto {
                vectors.vectors.resize_with(upto, || None);
            }
            let mut fd_iter = msg.fds.drain(..);
            for i in 0..need {
                let slot = (start as usize) + i;
                vectors.vectors[slot] = fd_iter.next();
            }
            tracing::info!(start, count, total = vectors.vectors.len(),
                "SET_IRQS: MSI-X trigger eventfds assigned");
            reply_ok(stream, msg_id, no_reply, "assign")
        }
        SetIrqsAction::Deassign { start, count } => {
            let need = count as usize;
            let upto = (start as usize) + need;
            if vectors.vectors.len() < upto {
                vectors.vectors.resize_with(upto, || None);
            }
            for i in 0..need {
                let slot = (start as usize) + i;
                vectors.vectors[slot] = None;
            }
            tracing::info!(start, count, total = vectors.vectors.len(),
                "SET_IRQS: MSI-X trigger eventfds de-assigned (all -1 / masked)");
            reply_ok(stream, msg_id, no_reply, "deassign")
        }
        SetIrqsAction::InvalidPartialFds { need, got } => {
            tracing::warn!(need, fd_cnt = got,
                "SET_IRQS DATA_EVENTFD: 部分 fd（0<fd_cnt<count），非法");
            send_err(stream, msg_id, libc::EINVAL as u32, no_reply)?;
            Ok(())
        }
        SetIrqsAction::BestEffortUnsupported => {
            tracing::debug!(flags = format_args!("{flags:#x}"),
                "SET_IRQS: unsupported flag combo — best-effort OK reply");
            reply_ok(stream, msg_id, no_reply, "best-effort")
        }
    }
```

**注意行为等价点**：
- 原 Assign 分支在 `need>0 && fd.len()==need` 内做 `resize_with`；新 Deassign 分支原本在 `is_data_eventfd && is_trigger` 块内**共享**了 L150-152 的 resize（在 fd 判断之前）。新代码把 resize 放进 Assign 和 Deassign 各自的 arm，保证 `vectors[slot]=None` 不越界——**这是行为保留的关键**，B-5 会专门回归。
- **InvalidPartialFds resize 消失（architect C-1）**：原码在 `is_data_eventfd && is_trigger` 块开头无条件 resize（含 partial-fd 非法路径也 resize 了再 send_err）。新码 InvalidPartialFds arm **不 resize** 直接 send_err。这是有意简化——原 resize 对一个即将返 EINVAL 的请求扩 vectors 数组是无意义副作用，外部可观察行为（EINVAL）不变。**非遗漏，是清理。**

- [ ] **B-4: transport irq 测试 + clippy**

```bash
cd /home/xp/refs/openvmm/usnvmemu/crates/vfio_user_transport && \
  cargo test --lib irq 2>&1 | tail -5 && \
  cargo clippy --lib --tests -- -D warnings 2>&1 | tail -3
```
预期：8 个 irq 集成测全 passed + clippy clean。

- [ ] **B-5: 加一个 transport-side 回归测（count=0 & EVENTFD corner）**

在 transport/irq.rs 的 `#[cfg(test)] mod tests` 加一个测试，验证 `DATA_EVENTFD | TRIGGER` + `count=0` + `fd_count=0` 走 deassign 空范围（不 panic、不越界、reply ok "deassign"）——防 B-3 误归类。命名 `set_irqs_eventfd_count_zero_deassigns_empty_range`。参照现有 `set_irqs_data_none_clears_vectors`（L327）的 socketpair 结构写。

```bash
cd /home/xp/refs/openvmm/usnvmemu/crates/vfio_user_transport && \
  cargo test --lib irq 2>&1 | tail -3
```
预期：9 个 irq 测全过。Track B 完成，**不 commit**。

---

## Track A — Message 拆 WireMessage + fds

**Files:**
- Create: `usnvmemu/crates/vfio_user_wire/src/framing.rs`（仅 `WireMessage` 类型，无 IO）
- Modify: `usnvmemu/crates/vfio_user_wire/src/lib.rs`（加 `pub mod framing;`）
- Modify: `usnvmemu/crates/vfio_user_transport/src/framing.rs`（`Message` 嵌 `WireMessage` + `Deref`）

**设计（Deref 最小破坏方案）**：
- wire crate `WireMessage { header: Header, payload: Vec<u8> }`（sans-IO，可 Clone）
- transport `Message { wire: WireMessage, fds: Vec<OwnedFd> }` + `impl Deref<Target=WireMessage>` + `DerefMut`
- 所有现有 `msg.header` / `msg.payload` 通过 Deref 透传，**零改动**；`msg.fds` 是外层字段，照常访问
- 只需改：`Message` struct 定义、`read_message` 的 `Ok(Message {...})` literal、Debug impl、3 处 `Message{...}` literal（framing.rs read 出口 + dma.rs 两个测试 mock）

- [ ] **A-1: 写 vfio_user_wire/src/framing.rs**

```rust
// Copyright (c) Microsoft Corporation.
// Licensed under the MIT License.

//! sans-IO 线消息类型。
//!
//! [`WireMessage`] = vfio-user 消息的**纯数据**部分（header + payload），
//! 不含任何 IO 句柄。fd（SCM_RIGHTS 传递的 OwnedFd）由 transport 端的
//! `Message` 包装，因为 fd 是平台 IO 资源、wire crate 不该见。
//!
//! 抽自 `vfio_user_transport::framing::Message`（W0.5，2026-06-11）——为
//! W1 underhill client 复用 wire 类型 + sans-IO 序列化做准备。

use crate::proto::Header;

/// vfio-user 线消息的纯数据部分（header + payload），不含 fd。
///
/// transport 端用 `Message { wire: WireMessage, fds }` 包装并经 `Deref`
/// 暴露 `header` / `payload`。
#[derive(Clone)]
pub struct WireMessage {
    /// 16 字节定长消息头。
    pub header: Header,
    /// payload 字节（不含 header）。
    pub payload: Vec<u8>,
}

impl std::fmt::Debug for WireMessage {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.debug_struct("WireMessage")
            .field("header", &self.header)
            .field("payload_len", &self.payload.len())
            .finish()
    }
}
```

- [ ] **A-2: lib.rs 加 pub mod + re-export + 跑测**

`vfio_user_wire/src/lib.rs` 加 `pub mod framing;`，更新 crate doc。

```bash
cd /home/xp/refs/openvmm/usnvmemu/crates/vfio_user_wire && cargo test --lib 2>&1 | tail -3
```
预期：39 passed（WireMessage 无新测，类型定义编译通过即可）。

- [ ] **A-3: transport/framing.rs 改 Message 为嵌套 + Deref**

编辑 `usnvmemu/crates/vfio_user_transport/src/framing.rs`：

把 `Message` struct（L54-61）改为：
```rust
pub use vfio_user_wire::framing::WireMessage;

/// transport 端线消息：wire 数据 + SCM_RIGHTS 传来的 fd。
pub struct Message {
    /// 纯数据部分（header + payload），经 [`Deref`] 暴露。
    pub wire: WireMessage,
    /// SCM_RIGHTS 传递的 fd（DMA_MAP / SET_IRQS 携带）。
    pub fds: Vec<OwnedFd>,
}

impl std::ops::Deref for Message {
    type Target = WireMessage;
    fn deref(&self) -> &WireMessage {
        &self.wire
    }
}

impl std::ops::DerefMut for Message {
    fn deref_mut(&mut self) -> &mut WireMessage {
        &mut self.wire
    }
}
```

改 Debug impl（L63-）：透传到 wire + 打 fd_count：
```rust
impl std::fmt::Debug for Message {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        f.debug_struct("Message")
            .field("header", &self.wire.header)
            .field("payload_len", &self.wire.payload.len())
            .field("fd_count", &self.fds.len())
            .finish()
    }
}
```

改 `read_message` 的 `Ok(Message {...})` 出口（L186）：
```rust
    Ok(Message {
        wire: WireMessage { header, payload },
        fds,
    })
```
（原变量名 `header`/`payload`/`fds` 沿用，按实际 read_message 局部变量名调整。）

- [ ] **A-4: dma.rs 2 个 Message literal 改造**

`usnvmemu/crates/vfio_user_transport/src/dma.rs` 两处测试 mock 构造 `Message { header, payload, fds }`（explore 指 L1278、L1450 附近，实际 grep 确认）：
```bash
grep -n "Message {" /home/xp/refs/openvmm/usnvmemu/crates/vfio_user_transport/src/dma.rs
```
每处改为 `Message { wire: WireMessage { header, payload }, fds }`。需 `use crate::framing::WireMessage;`（或 `use vfio_user_wire::framing::WireMessage;`）在 dma.rs 测试 mod。

- [ ] **A-5: 全 transport 编译 + 测试**

```bash
cd /home/xp/refs/openvmm/usnvmemu/crates/vfio_user_transport && \
  cargo build --lib 2>&1 | tail -3 && \
  cargo test --lib 2>&1 | tail -5
```
预期：build clean（Deref 让 session/handshake/irq 的 `msg.header`/`msg.payload` 零改动通过）+ 69+1（B-5 加的）= **70 passed**。

若编译报 `msg.fds` 相关错误（Deref 只透传 wire，fds 是外层字段）：`msg.fds.drain(..)` / `msg.fds.is_empty()` / `msg.fds.len()` 都该正常（外层字段直接访问）。若报 `msg.payload` 找不到——检查 DerefMut 是否正确。

- [ ] **A-6: lib.rs re-export WireMessage（可选）**

`usnvmemu/crates/vfio_user_transport/src/lib.rs` 若已 `pub use framing::Message`，加 `pub use framing::WireMessage;` 保持对称（下游目前不用，但 W1 client 会）。Track A 完成，**不 commit**。

---

## Final — 全套 oracle + 单 commit

- [ ] **F-1: 三 crate 全测**

```bash
cd /home/xp/refs/openvmm/usnvmemu/crates/vfio_user_wire && cargo test --lib 2>&1 | tail -2
cd /home/xp/refs/openvmm/usnvmemu/crates/vfio_user_transport && cargo test --lib 2>&1 | tail -2
cd /home/xp/refs/openvmm/usnvmemu/crates/pcie_device_core && cargo test --lib 2>&1 | tail -2
cd /home/xp/refs/openvmm/usnvmemu/crates/nvme_of_tcp_target && cargo test --lib 2>&1 | tail -2
```
预期：wire **39**（31+8 irq pure 测）、transport **70**（69+1 B-5）、pcie_device_core 原+2、nvme_of_tcp_target ~172 不退化。**实测 baseline 后核对，不信写死数字。**

- [ ] **F-2: clippy 全 clean**

```bash
for c in vfio_user_wire vfio_user_transport pcie_device_core; do
  cd /home/xp/refs/openvmm/usnvmemu/crates/$c && \
    echo "=== $c ===" && cargo clippy --lib --tests -- -D warnings 2>&1 | tail -2
done
```
预期：3 个都 clean。

- [ ] **F-3: nvme_firmware bin + POC 烟测**

```bash
cd /home/xp/refs/openvmm/usnvmemu/crates/nvme_firmware && cargo build --bin nvme_firmware 2>&1 | tail -2
POC_SKIP_BUILD=1 python3 /home/xp/refs/openvmm/usnvmemu/experiments/2026-06-11-openhcl-vfio-user-client-poc/poc1_dma_zerocopy.py 2>&1 | tail -5
POC_SKIP_BUILD=1 python3 /home/xp/refs/openvmm/usnvmemu/experiments/2026-06-11-openhcl-vfio-user-client-poc/poc2_msix_eventfd.py 2>&1 | tail -5
```
预期：bin clean + POC-1 PASS + POC-2 PASS（SET_IRQS 决策重构后 MSI-X 路径必须仍 PASS）。

若 POC-2 fail：SET_IRQS 决策重构有行为漂移，回退 Track B 重查。

- [ ] **F-4: nvme_of_tcp_target integration test**

```bash
cd /home/xp/refs/openvmm/usnvmemu/crates/nvme_of_tcp_target && \
  cargo test --test v8e7_2_async_fabric 2>&1 | tail -3
```
预期：passed（NoopTransport 迁移后路径不破）。

- [ ] **F-5: 单 commit（精确 pathspec）**

先看 staged/unstaged 全貌，确认不吞别会话改动（DECISIONS.md）：
```bash
cd /home/xp/refs/openvmm && git status --short
```

精确 add W0.5 范围（**不** `git add -A`）：
```bash
cd /home/xp/refs/openvmm
git add usnvmemu/crates/vfio_user_wire/src/irq.rs \
        usnvmemu/crates/vfio_user_wire/src/framing.rs \
        usnvmemu/crates/vfio_user_wire/src/lib.rs \
        usnvmemu/crates/vfio_user_transport/src/irq.rs \
        usnvmemu/crates/vfio_user_transport/src/framing.rs \
        usnvmemu/crates/vfio_user_transport/src/dma.rs \
        usnvmemu/crates/vfio_user_transport/src/lib.rs \
        usnvmemu/crates/pcie_device_core/src/noop_transport.rs \
        usnvmemu/crates/pcie_device_core/src/lib.rs \
        usnvmemu/crates/nvme_of_tcp_target/src/session.rs \
        usnvmemu/crates/nvme_of_tcp_target/src/async_session.rs \
        usnvmemu/crates/nvme_of_tcp_target/tests/v8e7_2_async_fabric.rs \
        usnvmemu/crates/nvme_of_tcp_target/Cargo.toml
git rm usnvmemu/crates/vfio_user_transport/src/transport.rs
```

确认 staged 不含 DECISIONS.md：
```bash
git diff --cached --name-only | grep -i decisions && echo "ERROR: DECISIONS.md staged!" || echo "OK: no DECISIONS.md"
```

**F-2（architect）Cargo.lock 连带**：删 nvme_of_tcp_target 的 vfio_user_transport dep 会改 lockfile。看 `git status --short` 里有没有 W0.5 引起的 `Cargo.lock`（usnvmemu crate 各自独立 lock 或 workspace 根 lock），若有且确属本次 dep 删除引起，一并 add（注意区分别会话的 poc6 untracked Cargo.lock，**不**碰它）：
```bash
cd /home/xp/refs/openvmm && git status --short | grep -i 'Cargo.lock'
# 对确属本次的 lock：git add <path>
```

commit：
```bash
git commit -m "refactor(vfio_user_wire): W0.5 cleanup — WireMessage 拆分 + irq 决策 pure 化 + NoopTransport 迁出

W0 收尾的三件 cleanup（spec §9 audit + W0 plan Open risks），合成单 commit：

A. framing::Message 拆 sans-IO WireMessage
   - vfio_user_wire 新增 framing 模块：WireMessage { header, payload }（无 fd）
   - vfio_user_transport Message = { wire: WireMessage, fds } + Deref/DerefMut
     → 所有 msg.header / msg.payload caller 经 Deref 零改动
   - fd（OwnedFd）留 transport 外层字段，wire crate 不见 IO 句柄
   - 为 W1 underhill client 复用 wire 消息类型铺路

B. decide_set_irqs_action pure 化
   - vfio_user_wire 新增 irq 模块：SetIrqsAction enum + decide_set_irqs_action
     纯决策（6 分支：BestEffortNonMsix / ClearAll / Assign / Deassign /
     InvalidPartialFds / BestEffortUnsupported），7 个 pure-fn 单测
   - vfio_user_transport handle_set_irqs 改 match 决策 + 分发 IO；保留真
     QEMU 11 oracle 的 fd_count 灰区语义（fd==count assign / fd==0 deassign /
     0<fd<count 非法）；加 count=0+EVENTFD deassign 空范围回归测

C. NoopTransport 迁到 pcie_device_core
   - 从 vfio_user_transport 迁出（删 transport.rs）→ pcie_device_core
     silent stub（去 tracing，保持 pcie_device_core 零依赖）
   - nvme_of_tcp_target 解除对整个 vfio_user_transport 的反向 dep
     （之前仅为 1 个 ZST 拉全套），3 处 import 改 pcie_device_core::NoopTransport

oracle：
- vfio_user_wire 38（31+7）/ vfio_user_transport 70（69+1）/ pcie_device_core +2 /
  nvme_of_tcp_target 不退化
- POC-1（DMA 零拷贝）PASS / POC-2（MSI-X eventfd，验 SET_IRQS 重构）PASS
- clippy --lib --tests -D warnings clean（三 crate）
- nvme_firmware bin clean

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
git log --oneline -1
```

---

## Self-Review

**1. Spec 覆盖**：spec §9 audit "Message vs WireMessage 拆分" → Track A；W0 plan Open risks 的 decide_set_irqs_action pure 化 → Track B；NoopTransport 迁出 → Track C。三件全覆盖。

**2. Placeholder 扫描**：无 TBD/later。所有命令真路径，所有新代码完整给出。grep 命令用于确认行号（explore 行号可能漂）。

**3. 类型一致性**：
- `SetIrqsAction` 6 变体 B-1 定义 / B-3 match，全覆盖（含 Assign/Deassign 的 `{start, count}`、InvalidPartialFds 的 `{need, got}`）
- `WireMessage { header: Header, payload: Vec<u8> }` A-1 定义 / A-3 transport 嵌套 / A-4 dma literal，字段名一致
- `decide_set_irqs_action(idx, flags, start, count, fd_count)` B-1 签名 / B-3 调用一致
- `Message { wire, fds }` + Deref：A-3 定义后所有 `msg.header`/`msg.payload` 透传，`msg.fds` 外层字段

**4. 单 commit 纪律**：每 Track 末尾标"不 commit"；F-5 一次性精确 pathspec commit + DECISIONS.md 防吞检查。符合用户 commit 粒度要求。

**5. 行为等价风险点**（已在 plan 内标）：
- B-3 的 resize 从"EVENTFD 块共享"挪到 Assign/Deassign 各 arm → B-5 专门回归
- A Deref 方案让 caller 零改动 → A-5 编译即验证

---

**Plan 完。** 3 Track + Final = 4 段共 ~18 步，单 commit 收口。
